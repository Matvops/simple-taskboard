import type { TasksType } from "../../types/TasksTypes";

export const initialState: TasksType = {
   tasks: [
    {
        name: 'Teste 11111111',
        priority: 'lowPriority',
        stage: 'toDo',
        createdAt: 0
    }
   ]
}