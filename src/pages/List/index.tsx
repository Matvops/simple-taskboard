import { Table } from "../../components/Table";
import { MainTemplate } from "../../templates/MainTemplate";

export const List = () => {

  return (
    <>
      <MainTemplate>
        <Table />
      </MainTemplate>
    </>
  );
}